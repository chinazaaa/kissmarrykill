// Client-safe community leaderboard constants (no server-only imports).

// Default community WhatsApp invite link. Used as a fallback when the admin
// hasn't set a custom link in /admin/community, so the "Join the community"
// prompts always work. The admin setting overrides this when present.
export const DEFAULT_WHATSAPP_INVITE_URL = 'https://chat.whatsapp.com/Kz92J6xr7g7CwqlRZCuKFM'
