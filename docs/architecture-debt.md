# Architecture & SOLID debt tracker

Living checklist from the 2026-06 architecture audit (3 parallel reviews over `dev`). Tick items as they land. Severity = impact, Effort = S/M/L.

**Root cause:** there is no game registry — each of ~29 games is wired by hand-maintained `if (isXGame…)` chains, so adding one game touches **~22 files / 40+ edit-sites**. Most items below are symptoms of that. Recommended order is top-to-bottom (bugs → tests → exhaustiveness → registry → decomposition); the later structural work is unsafe without the earlier safety nets.

Legend: `[ ]` todo · `[x]` done · `[~]` in progress

---

## Phase 0 — Quick-win bugs (isolated, low-risk, ship now)

- [x] **`feedbackGameTypeEnum` was stale** → fixed. It omitted 9 games (chess, scrabble, sudoku, tic_tac_toe, word_hunt, pick_a_number, i_call_on, describe_it, snake_and_ladder), so feedback for them was rejected. Now derived from `gameTypeEnum.options` (+ `'general'`) so it can't drift. `src/lib/validation.ts`.
- [x] **Copy-paste bug** → fixed. Removed the duplicated identical `isICallOnGame` payload spread in `src/app/create/page.tsx`.
- [x] **Unguarded `await req.json()` → 500 instead of 400.** Added `parseJsonBody(req, schema)` helper (`src/lib/parse-body.ts`, try/catch + safeParse). Adopted in `finish-game` (reference) + the 9 one-liner routes, then the **two-step** `const raw = await req.json(); schema.safeParse(raw)` form across **93 routes** — all used the identical `issues[0].message`/400 shape parseJsonBody produces, so behaviour-identical for valid requests (only malformed/empty bodies change 500→400). Adoption: 10 → 103 routes. _Left as bespoke (genuinely not a single-schema parse): `monopoly/trade` (4-schema branch dispatch), `games` POST (raw body reused for `elimination_config`), `games/[code]/lobby-settings` (merges route `code` into the body); plus ~15 routes that read `req.json()` with no schema (separate concern)._ **Sev High · Eff M — ✅ done**
- [x] **"Room points not awarded" — investigated, FALSE POSITIVE.** The audit grep matched `status:'finished'` on the **rounds** table (`finish-game:48`, `end-round:57`) and the **tournaments** table (`tournaments/[code]/finish`), not the `games` table. The actual game-finish path already routes through `markGameFinished` (`finish-game:115`). _Open sub-question (separate item): confirm each game-specific early-finish fn (`finishMonopolyGameEarly`, `finishScrabbleGameEarly`, `finishCodewordsGame`, `finishAnonymousRoomSession`, `finishSecretMessageBoard`) awards room points._
- [ ] **Duplicate realtime channels.** `useGameChannel` (setState) and `useGameRealtime` (react-query invalidation) are _both_ mounted in `host/[code]/page.tsx` on the same tables (half-finished migration). → Pick one, delete the other. _Deferred: it's a frontend realtime behavior change — do carefully (ideally after Phase 1 tests)._ **Sev Med · Eff S–M**

## Phase 1 — Safety net (prerequisite for everything below)

- [x] **No automated tests** → fixed (#144). Added Vitest (node env) + a CI `Test` job; 29 unit tests over pure logic (scrabble scoring/geometry, language tile sets, `parseGameType`/guards, round generation). The harness everything below relies on.
  - [x] **Component/hook harness** — added jsdom + React Testing Library + `@vitejs/plugin-react`, with a dummy-Supabase `test.env` so components that build a client at import don't throw. Per-file `// @vitest-environment jsdom` opt-in keeps pure-logic tests on node. This is the prerequisite for safely doing the **Phase 4 god-file decomposition** & realtime-view refactors.

## Phase 2 — Make dispatch fail-fast (cheap safety, surfaces existing gaps)

- [~] **Silent-fallthrough dispatch.** Delivered a fail-fast **coverage guard** instead (#147): a test that fails CI when a new game is half-wired across the hand-maintained surfaces. _The audit's `switch + assertNever` does **not** fit these chains — they have a legitimate default (poll-family games render inline), so a naive `assertNever` would be wrong. Consolidating the dispatch is folded into Phase 3 (registry)._
- [x] **Partial enums drift from `GameType`** → addressed. `feedbackGameTypeEnum` now derives from `gameTypeEnum.options` (#142); `GAME_TYPE_OPTIONS`, `gameTypeEnum` (via `createGameSchema`), config, and landing slug/content/rules drift is now caught by the Phase 2 coverage test (#147). _`LOBBY_LIMIT_GAME_TYPES` is an intentional subset, left as-is._

## Phase 3 — Game registry (the structural fix)

- [~] **Introduce `GAME_REGISTRY: Record<GameType, GameDefinition>`** carrying `{ config, slug, landing, rules, selects, maxPlayers, validation, initialize, clearSession, canPlayAgain, PlayerView, HostView, score }`. Migrate incrementally: **(a)** client view dispatch first (mechanical — the views take uniform props: player views `gameCode`, host views `gameCode` + `hostToken`), **(b)** config/landing/selects, **(c)** server `start`/`clearSession`. Collapses ~22 edit-sites → ~1. **Sev High · Eff L**
  - [x] **(a) client view dispatch** — `PLAYER_VIEW_REGISTRY` (`game-player-views.ts`) + `HOST_VIEW_REGISTRY` (`game-host-views.ts`) replace the two 18-branch `if (isXGame) return <XView/>` chains with a `parseGameType` lookup (behavior-identical: every guard was `parseGameType(x) === literal`). Removed ~196 lines from the two god files; adding a game's views is now one registry entry each.
  - [~] **(c) server `clearSession`** — Play Again's 15-branch `if (isXGame) clear*SessionData()` chain replaced by a `SESSION_CLEARERS` registry lookup (one clearer runs per request, so behavior-identical). `trivia`/`anonymous`/`secret_message` stay special (delete-order / anon-client / early-return). _(The sibling `start/route.ts` consolidation is now done — see (c) server `start` below.)_
  - [x] **(c) server `start`** — the 9 uniform "board game" branches (monopoly, yahtzee, whot, crazy_eights, ludo, snake_and_ladder, tic_tac_toe, chess, scrabble) in `start/route.ts` — each a copy-paste of filter-spectators → count-check → `initialize*Game` → activate — collapsed into a `GAME_START_SPECS` registry + a generic dispatch (`game-start.ts`, tested). Only the count rule (`atLeast`/`exact`/`range`) and the `initialize*` call differ, so they're data now. −221 lines from the route; behaviour-identical. Games with bespoke start logic (trivia/bingo/describe-it/codewords/two-truths/i-call-on/sudoku/word-hunt/anonymous/secret-message/custom) keep their own branches, same as `clearSession`.
  - [ ] (b) config/landing/selects
- [~] **`GameEngine` interface + `clearSessionTables(supabase, gameId, tables[])` helper** — replaces ~12 copy-pasted `clear*SessionData` and the duplicated `initialize*Game`/`can*PlayAgain` shapes; fixes `{error?}` vs `{error:null}` return drift. **Sev Med-High · Eff M**
  - [x] **`clearSessionTables` helper** (`session-clear.ts`) — the 12 table-deleting `clear*SessionData` functions (± a spectator reset) are now one-liners delegating to it; unified their return type to `{ error: string | null }`. −77 lines; verified behaviour-identical with a mock-Supabase test. (3 no-op stubs + `describe_it`'s pool-usage logic left as-is.)
  - [ ] `GameEngine` interface unifying `initialize*Game`/`can*PlayAgain` shapes

## Phase 4 — Decompose god files & adopt existing abstractions

- [~] **God files (SRP):** `host/[code]/page.tsx` (4.3k) → extract poll-host body into `PollHostView`; `create/page.tsx` (3.3k) → per-game settings registry + shared `<MaxPlayersField>`/`<TurnTimerField>`/`<LateJoinField>`; `PollGamePlayerExperience.tsx` (3.2k, 16 forward-refs) → split into `PollLobby/Round/Results/FinalResults`; `useGameSession.ts` (767) → split into join/realtime/voting/timers hooks; `start/route.ts` (1.6k) and `monopoly.ts` (2.8k) → rules/store/messages split; `types/index.ts` (915) → co-locate per-game types with engines. **Sev High · Eff L**
  - [x] **`host/[code]/page.tsx` → thin dispatcher + `PollHostView`** (#168, test-first). The page dropped **~4,180 → 122 lines**: it now verifies the host token, resolves the game type, and dispatches to the board host view (registry) or `<PollHostView gameCode hostToken />`. The whole poll-host machinery (state / realtime / handlers / render, incl. the extracted `WstQuotePoolStatus` leaf) moved verbatim into `PollHostView`, which owns its own loader exactly like the board views. Verified by tsc + `next build` + the component harness; behaviour-equivalent by construction (relocated code, props instead of params). _Needs manual host-page QA before merge: auth, board-game dispatch, and a poll game through lobby → active → finished._ The other god files (`create` page, `PollGamePlayerExperience`, `useGameSession`, …) remain — and `PollHostView` itself can now be split further with the harness.
- [~] **Consolidate the copy-pasted realtime channels** — ~38 game views each hand-rolled `supabase.channel().on('postgres_changes', …).subscribe()` + a debounce (~850 LOC). Introduced `useGameTableSync(gameCode, tables, reload)` (#181, tested) — bare-string tables filter by `game_id`, `{ table:'games', column:'id' }` by `id`. **Migrated 25 views** (scrabble #181 + 24 board/round views in the rollout). _Intentionally left hand-rolled (not behaviour-equivalent to a plain reload): **bingo** H/P (optimistic incremental `setCalledNumbers` updates), **secret-message Sender** (extra `clearPlayerSession`/`setMyPlayerId` side-effects), **sudoku** & **word-hunt** H/P (deliberately granular — incremental `setSubmissions` appends on a `round_id`-scoped channel, specifically to avoid `load()`'s per-event grid rebuild from localStorage + `sudoku_solved_cells`; investigated 2026-06, **decided to keep hand-rolled** — a hook migration would risk a mid-puzzle gameplay regression and there's no polling fallback / test to catch it), **describe-it** (different pattern), **anonymous-messages** + **RoomLobby** (broadcast/presence), **RoomsPage** (not game-scoped)._ Still open: the `useGameViewBootstrap()` (load + screen-state + join) scaffold for the ~33 duplicated view shells. **Sev High · Eff M**
- [~] **Collapse hook clone families** — generic `useTurnTimer(config)` (7+ clones), `useAdvancePolling(config)` (3), `useGameExpiryTimer(config)` (4), `useGameEventNotifier(...)` (7); hoist `secondsUntil`/`formatCountdown` to `lib/`. ~700 LOC. **Sev Med · Eff M**
  - [x] **Hoisted the pure timer helpers** to `src/lib/timer-format.ts` (tested): `secondsUntil` (was copy-pasted in 10 hooks), `formatCountdown` (hours-aware, 5 hooks), and `formatMinutesSeconds` (minutes-only — kept distinct so the 2 hooks using it don't gain an hours segment). 17 call-sites now import it; −84 lines. The hook _bodies_ (timer/polling/notifier scaffolds) are the larger remaining clone work.
- [~] **Centralize Supabase client + host-auth.** 121 routes each `createClient(... ANON_KEY)` on privileged mutations → one `getClient()`/deliberate anon-vs-service-role; collapse 4 `assertHost*` into one `assertHost(...)` (27 hand-rolled checks). **Sev High · Eff M**
  - [x] **`assertHost*` consolidation** — the 4 near-identical `assertHost*` fns in `game-admin.ts` now delegate to one private `assertHost(…, { allowedStatuses, statusError })`; public signatures unchanged (callers untouched), behaviour verified with a mock-Supabase test. _Still TODO: the 27 hand-rolled inline `host_token !==` checks, and the 121 inline `createClient` calls._
- [ ] **`validation.ts` (1062 lines) split per-domain**; add schemas to the ~34 unvalidated routes (`rooms/*`, `admin/*`, `library/*`). **Sev Med · Eff M**
- [ ] **`supabase-selects.ts` is effectively dead** (0 route importers; ~74 `select('*')` in routes) — adopt broadly or delete; consider `supabase gen types`. **Sev Low · Eff S–M**
- [ ] **Merge `game-landing.ts` + `game-landing-rules.ts`** (must be edited in lockstep; rules vs FAQ prose duplicate facts). Note: these `Record<GameType,…>` maps are the _one_ exhaustive (good) pattern — model the registry on them. **Sev Low · Eff S**

---

_Audit basis: `origin/dev` as of 2026-06. Update this file as items land (link the PR next to each)._
