# Supabase CLI config. Used by the "DB Migrate" CI job (supabase link + db push)
# to apply supabase/migrations.
#
# Two-project setup (not Branching):
#   - prod = skhvbzitwvnbhqxfitgh — migrated by the CI job on push to `main`.
#   - dev  = its own project in a separate free org. NOT yet wired: dev pushes
#     currently skip migration until its ref + secrets are configured.
# project_id below is only the default/fallback for local CLI use; the migrate job
# links the target project explicitly via `supabase link --project-ref`.

project_id = "skhvbzitwvnbhqxfitgh"

[db]
major_version = 17
